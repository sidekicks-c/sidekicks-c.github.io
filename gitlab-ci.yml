#image: registry.git.cccv.de/infra/static-pages/docker-images/buster
image: debian:buster

pages:
  stage: deploy
  script:
    - mkdir public
    - mv static public
    - mv congress.html public/index.html
  artifacts:
    paths:
    - public
  only:
  - master

