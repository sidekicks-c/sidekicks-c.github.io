#image: registry.git.cccv.de/infra/static-pages/docker-images/buster
image: debian:buster

pages:
  stage: deploy
  script:
  - echo 'Nothing to do...'
  artifacts:
    paths:
    - public
  only:
  - master
